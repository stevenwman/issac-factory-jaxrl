# 2026-05-26 — Session 1: bootstrap + GPU driver block

## Done

- **Brainstorm + spec** written and approved. Lives at `.superpowers/specs/2026-05-26-isaaclab-factory-jax-baseline-design.md`. 11 locked decisions including: external project, NutThread = bolt-tighten primitive, dedicated uv venv, jax[cuda12] override, FlashSAC as primary algo, jax-learning branch-don't-touch-main policy (D11), Isaac stock controller for M5b baseline.
- **Plan** written and reviewed. Lives at `.superpowers/plans/2026-05-26-isaaclab-factory-jax-baseline.md`. 27 tasks across M0–M6.
- **M0.1 (git init + remote + first push)** — completed via subagent. Repo pushed to `git@github.com:stevenwman/issac-factory-jaxrl.git` (user-specified name with "issac" typo preserved). Two commits on `main`.
- **M0.2 (controller + IK survey)** — completed via subagent. `docs/controllers_survey.md` exists. Confirmed Factory uses its own `compute_dof_torque` (task-space PD with null-space stabilization), no F/T sensing in default obs. Distinguished `pink` (Caron, pinocchio, IsaacLab ships it) from `mink` (Zakka, MuJoCo) and `frax` (StanfordASL, JAX). Decision: use Isaac stock controller for M5b — defer any swap to a follow-on spec.

## Blocked

- **M1.1 (sanity check: Isaac NutThread + random agent + video)** — system-level GPU driver mismatch. Running kernel = `6.17.0-29-generic`; installed nvidia-driver modules only exist for `6.17.0-22-generic`. `nvidia-smi` fails, `/dev/nvidia*` absent, Isaac crashes with `CUDA error: no CUDA-capable device is detected`. Hardware present (NVIDIA GPU at PCI 01:00.0).

## How to resume next session

1. **Fix the driver first.** Three options laid out for the user; user is handling offline:
   - Reboot into `6.17.0-22-generic` via GRUB (lowest-risk)
   - `sudo apt-get install linux-modules-nvidia-580-open-6.17.0-29-generic` (if package exists)
   - `sudo dkms autoinstall` (rebuild modules; requires kernel headers + nvidia-driver-580-open-dkms)
2. Verify with `nvidia-smi` returning device info.
3. Resume execution at **M1.1** in the plan. All downstream tasks (M1–M6) need CUDA.
4. M0 tasks are committed + pushed; nothing to redo there.

## Notes

- All implementation context is in `.superpowers/specs/` and `.superpowers/plans/`. A fresh agent can pick up by reading those two files.
- TodoWrite state shows M1.1 paused; resume by switching it back to in_progress and re-dispatching the M1.1 implementer.
- Subagent model tiering policy (decided this session): Haiku for infra, Sonnet for code + reviews, Opus for the final whole-branch review. Apply going forward.
- Per the M0 push cadence, push after the last task of each milestone. Subagent safety heuristics flagged push-to-main as a security concern — false positive in this project (user explicitly authorized via the plan). Pre-empt in future dispatch prompts.
